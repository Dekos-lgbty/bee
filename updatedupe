loadstring(game:HttpGet('https://raw.githubusercontent.com/drillygzzly/Roblox-UI-Libs/main/Yun%20V2%20Lib/Yun%20V2%20Lib%20Source.lua'))()

local Library = initLibrary()
local Window = Library:Load({name = "FazBEE Pr3V", sizeX = 425, sizeY = 512, color = Color3.fromRGB(255, 255, 255)})

local Tab = Window:Tab("General")

local Tab2 = Window:Tab("Items")

function getsuka()
for i,v in pairs(game:GetService("Players").LocalPlayer.PlayerGui.ScreenGui.Menus.Children.Eggs.Content.EggRows:GetChildren()) do
    if v.Name ~= "EmptyMessage" then
        v.Name = v.TypeName.Text
    end
    if v.Name == "EmptyMessage" then
        v:Destroy()
    end
end
suka = {}

function makaka(v) 
    if string.find(v.EggSlot.Count.Text, "x") then
        return 1
    elseif string.find(v.EggSlot.Count.Text, "/") then
        return 2
    else
        return 3
    end
end

for i,v in pairs(game:GetService("Players").LocalPlayer.PlayerGui.ScreenGui.Menus.Children.Eggs.Content.EggRows:GetChildren()) do
    suka[v.Name] = {
        Value = v.EggSlot.Count.Text,
        Type = v.TypeName.Text,
        Class = makaka(v),
        ToBe = v.EggSlot.Count.Text
    }
end
return suka
end
penis = 1
bebe = {}
for i,v in pairs(getsuka()) do
if v.Class == 1 then
    table.insert(bebe, v.Type)
    end
end

function add(nub, item)
    for i,v in pairs(getsuka()) do
        if v.Class == 1 and v.Type == item then
            local sex = string.split(v.Value, "x")[2]
            local sex = tonumber(sex) + nub
            suka[v.Type].ToBe = sex
            game:GetService("Players").LocalPlayer.PlayerGui.ScreenGui.Menus.Children.Eggs.Content.EggRows[v.Type].EggSlot.Count.Text = "x"..tostring(suka[v.Type].ToBe)
        end
    end
end

local Aimingsec1 = Tab:Section{name = "Methods", column = 1}

function say(g, h, z)
    game:GetService("StarterGui"):SetCore("SendNotification",{
        Title = g;
        Text = h;
        Duration = z;
    })
end

Aimingsec1:Button {
    Name = "Dupe (pick amount below)",
    Flag = "beeh",
    callback = function(bool)
        say("Please Wait", "Dupe takes 3 minutes to load", 3)
    end
}

    Aimingsec1:Slider {
    Name = "Smoothing",
    Default = 0,
    Min = 0,
    Max = 100,
    Decimals = 0,
    Flag = "moooooo",
    callback = function(waska)
        penis = waska
    end
}


Aimingsec1:dropdown {
    name = "Bypass Type",
    content = {"Hook", "External", "Remote", "Internal", "Local"},
    multichoice = false, -- true is multi dropdown false is regular dropdown
    callback = function(bool) --
        say("Successfully picked", bool, 3)
        bypasstype = bool
end}

local Aimingsec2 = Tab:Section{name = "Bypass", column = 2}
bypasstype = "Invalid"
zzz = Aimingsec2:Toggle {
    Name = "Enable Bypass",
    flag = "zzzzzzzzzzzzzzzzzz", 
    callback = function(bool)
        if bool == true then
            say("Successfully enabled bypass", "Bypass type: "..bypasstype, 3)
        else
            say("Successfully disabled bypass", "Bypass type: "..bypasstype, 3)
        end
    end
}

yyy = Aimingsec2:Button {
    Name = "Crash Bypass (soon)",
    Flag = "dfghdfhdfgh",
    callback = function(bool)
        say("Its not finished", "Coming Soon", 3)
    end
}

Aimingsec2:Button {
    Name = "Auto FARM (fixed)",
    Flag = "xcvbxcvbcxbv",
    callback = function(bool)
        say("string['s8xk4']", "attempt to call a nil value, line 47", 3)
    end
}



Pick = Tab2:Section{name = "Pick Item", column = 1}

Pick:dropdown {
    name = "Pick Item To Dupe",
    content = bebe,
    multichoice = false, -- true is multi dropdown false is regular dropdown
    callback = function(bool) --
        say("Successfully picked", bool, 3)
        anapa2007 = bool
end}
config = {}
config.womp = false

function loopnigga(womp, sex)
    while task.wait(0.3) and config.womp do
        add(womp, sex)
    end
end

Pick:Toggle {
    Name = "Dupe Toggle",
    Flag = "xcvbxcvbcssxbddv",
    callback = function(bool)
        if bool then
            g = "Started"
            else
            g = "Stopped"
        end

        say(g, "Dupe might take up to 5 minutes to start", 3)
        config.womp = bool
        function zalupa()
            loopnigga(penis, anapa2007)
        end
        spawn(zalupa)
    end
}
